{
  "name": "CryptoKitties",
  "version": "1.0.0",
  "description": "A simple implementation of CryptoKitties with more tricks.",
  "main": "truffle.js",
  "scripts": {
    "all": "./node_modules/.bin/truffle compile --compile-all && ./node_modules/.bin/truffle migrate --reset && cp -rf build src/ && cd src && node server.js",
    "deploy": "./node_modules/.bin/truffle compile --compile-all && ./node_modules/.bin/truffle migrate --reset",
    "dev": "cp -rf build src/ && cd src && node server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.16.3",
    "lite-server": "^2.3.0",
    "truffle": "^4.1.3"
  },
  "dependencies": {},
  "resolutions": {
    "**/**/fsevents": "^1.2.9"
  }
}
